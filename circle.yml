machine:
  node:
    version: 8.1.4

parallelism: 1

dependencies:
  cache_directories:
    - ~/.cache/yarn
  override:
    - yarn

deployment:
  development:
    branch: development

  production:
    branch: master